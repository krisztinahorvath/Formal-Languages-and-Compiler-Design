main
{
   integer a = 100;
   integer b;
   integer c;

   read(a);
   read(b);
   read(c);
 
   integer maxNr = a;
   if (maxNr < b) {
	maxNr = b;
    }
   if (maxNr < c) {
	maxNr = c;
    }

   b = -23a;
    write(maxNr);
}

