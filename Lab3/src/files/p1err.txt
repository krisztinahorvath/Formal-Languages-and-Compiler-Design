min // incorrect program definition
{
   integer 1a; // identifier cannot start with a number
   integer b;
   integer c;

   read(a);
   read(b);
   read(c);
 
   integer maxNr = a;
   if (maxNr < b) {
	maxNr = b;
    }
   if (maxNr < c) {
	maxNr = c;
    }

    write(maxNr);
}

